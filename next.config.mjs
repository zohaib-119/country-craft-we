/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["encrypted-tbn0.gstatic.com", "img.freepik.com", "cdn-icons-png.flaticon.com", "lh3.googleusercontent.com"]
    }
};

export default nextConfig;
